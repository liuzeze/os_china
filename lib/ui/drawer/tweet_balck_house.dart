import 'package:flutter/material.dart';

class TweetBlackHouse extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('动弹小黑屋'),
      ),
    );
  }
}
